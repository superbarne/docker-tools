name: Docker Image CI

on:
  push:
    branches:
      - master
    paths:
      - 'kubectl/*'
jobs:

  build:
 
    runs-on: ubuntu-latest
 
    steps:
    - uses: actions/checkout@v1
    - name: Build the Docker image
      run: cd kubectl && docker build . --file Dockerfile --tag barne/kubectl:$(date +%s)
